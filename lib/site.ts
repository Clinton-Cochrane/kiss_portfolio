/**
 * Production site URL for SEO (sitemap, Open Graph, canonical).
 * Set NEXT_PUBLIC_SITE_URL in your deployment environment or update the fallback.
 */
export const SITE_URL =
	process.env.NEXT_PUBLIC_SITE_URL ?? "https://clintoncochrane.com";
